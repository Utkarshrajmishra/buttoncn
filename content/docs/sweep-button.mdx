---
title: Sweep Button
description: A button with a glowing sweep animation that slides across on hover, featuring customizable sweep and background colors for a sharp, modern look.
---
import { TypeTable } from 'fumadocs-ui/components/type-table';

import {SweepButtonPreview, SweepButtonPreviewSweepColor,SweepButtonPreviewBackgroundColor} from "@/components/buttoncn-preview/sweep-button-preview";

<Preview link="sweep-button">
  <SweepButtonPreview />
</Preview>

## Installation

```npm
npx shadcn@latest add http://localhost/300/r/sweep-button.json
```

## Usage

```tsx title="usage.tsx"
import BrokenButton from "@/components/buttoncn/broken-button";

export default function Usage() {
  return <BrokenButton />;
}
```

## Examples

### Sweep Color

<Preview link="sweep-button">
  <SweepButtonPreviewSweepColor />
</Preview>

```tsx title="usage.tsx"
import SweepButton from "../buttoncn/sweep-button";

const SweepButtonPreviewSweepColor = () => {

//The border  inherits sweepColor value by default, but can be customized using className.

  return <SweepButton sweepColor="#a1f78dff">Sweep Button</SweepButton>;
};

export default SweepButtonPreviewSweepColor;
```

### Background Color

<Preview link="sweep-button">
  <SweepButtonPreviewBackgroundColor />
</Preview>

```tsx title="usage.tsx"
import SweepButton from "../buttoncn/sweep-button";

const SweepButtonPreviewSweepColor = () => {
  return <SweepButton backgroundColor="#4e2d0eff">Sweep Button</SweepButton>;
};

export default SweepButtonPreviewSweepColor;
```


## API Reference

### Sweep Button

The **Sweep Button** component is a wrapper around the **button** element that adds a variety of styles and functionality.

<TypeTable
  type={{
    sweepColor: {
description: 'Defines the color of the animated sweep wave and the button borders. The border color inherits this value by default, but can be customized using className.',
     
      type: 'hexcode',
      default: '#22d3ee',
    },
    backgroundColor: {
      description: 'Color of the button background',
     
      type: 'hexcode',
      default: '#083344',
    },
     className: {
      description: "Allows additional styling to be applied to the button.",

      type: "string",
},
  }}
/>




