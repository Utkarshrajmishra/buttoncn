---
title: Shadow Pop Button
description: A bold button that pops on hover with contrasting dual-color shadows, creating a punchy, retro-style depth effect.
---

import {
  ShadowPopButtonPreview,
  ShadowPopButtonPreviewShadowColors,
} from "@/components/buttoncn-preview/shadow-pop-button-preview";
import { TypeTable } from "fumadocs-ui/components/type-table";

<Preview link="shimmer-button">
  <ShadowPopButtonPreview />
</Preview>

## Installation

<Tabs items={['Command', 'Manual']}
 className="bg-transparent border-none"
>
  <Tab value="Command" className="bg-transparent">
  ```npm
 npx shadcn@latest add https://fluxbuttons.vercel.app/r/shadow-pop-button.json
```
  </Tab>
  <Tab value="Manual" className="bg-transparent border">
  ```tsx title="shadow-button.tsx"
"use client"

import * as React from "react"
import { cn } from "@/lib/utils"

export interface ShadowPopButtonProps
    extends React.ButtonHTMLAttributes<HTMLButtonElement> {
    shadowColors?: [string, string]
}

const ShadowPopButton = React.forwardRef<HTMLButtonElement, ShadowPopButtonProps>(
    ({ 
        className, 
        children, 
        shadowColors = ["#F44336", "#00BCD4"],
        ...props 
    }, ref) => {
        return (
            <button
                ref={ref}
                className={cn(
                    "cursor-pointer text-white uppercase bg-neutral-800  px-4 py-2 active:translate-x-0.5 active:translate-y-0.5 transition",
                    className
                )}
                style={{
                    boxShadow: undefined,
                }}
                onMouseEnter={(e) => {
                    e.currentTarget.style.boxShadow = `0.5rem 0.5rem ${shadowColors[0]}, -0.5rem -0.5rem ${shadowColors[1]}`
                }}
                onMouseLeave={(e) => {
                    e.currentTarget.style.boxShadow = "none"
                }}
                {...props}
            >
                {children || "Hover me"}
            </button>
        )
    }
)

ShadowPopButton.displayName = "ShadowPopButton"

export default ShadowPopButton
```
  </Tab>
</Tabs>


## Usage

```tsx title="usage.tsx"
import ShadowPopButton from "@/components/ui/shadow-pop-button";

export const ShadowPopButtonPreviewShadowColors = () => {
  return (
    <ShadowPopButton>
      Shadow Pop Button
    </ShadowPopButton>
  );
};
```

## Examples

### Shadow Color

<Preview link="shimmer-button">
  <ShadowPopButtonPreviewShadowColors />
</Preview>

```tsx title="usage.tsx"
import BrokenButton from "@/components/ui/broken-button";

export const ShadowPopButtonPreviewShadowColors = () => {
  return (
    <ShadowPopButton shadowColors={["#F44336", "#00BCD4"]}>
      Shadow Pop Button
    </ShadowPopButton>
  );
};
```

### API Reference

### Shadow Pop Button

<TypeTable
  type={{
    shadowColors: {
      description: "The color of the shadow. The array must be of size 2. (shadowColors[0] is bottom-level shadow and shadowColors[1] is top-level shadow) ",
      type: "hexcode array(2)",
      default: "['#366ff4ff', '#d45f00ff']",
    },
    className: {
      description: "Allows additional styling to be applied to the button.",

      type: "string",
    },

}}
/>
