---
title: Wave Button
description: Motion-enhanced button featuring a subtle animated wave pattern that flows across the surface on hover, adding energy, depth, and visual feedback without overpowering the UI.
---
import { TypeTable } from 'fumadocs-ui/components/type-table';

import {
  WaveButtonPreview,
  WaveButtonPreviewWaveColors,
} from "@/components/buttoncn-preview/wave-button-preview";

<Preview link="wave-button">
  <WaveButtonPreview />
</Preview>

## Installation

```npm
npx shadcn@latest add http://localhost/300/r/wave-button.json
```

## Usage

```tsx title="usage.tsx"
import BrokenButton from "@/components/buttoncn/broken-button";

export default function Usage() {
  return <BrokenButton />;
}
```

## Examples

### Wave Colors

<Preview link="wave-button">
  <WaveButtonPreviewWaveColors />
</Preview>

```tsx title="usage.tsx"
import WaveButton from "../buttoncn/wave-button";

const WaveButtonPreviewWaveColors = () => {
  return (
    <div>
      <WaveButton
        waveColors={["#7f1d1d", "#991b1b", "#b91c1c", "#dc2626", "#ef4444"]}
      >
        Wave Button
      </WaveButton>
    </div>
  );
};

export default WaveButtonPreviewWaveColors;
```

## API Reference

### Wave Button

The **Wave Button** component is a wrapper around the **button** element that adds a variety of styles and functionality.


<TypeTable
  type={{
    waveColors: {
description: 'Defines the color of the waves.',
     
      type: 'hexcode array[5] (array must contain 5 hexcode values)',
      default: '["#0c4a6e", "#075985", "#0369a1", "#0284c7", "#0ea5e9"]',
    },
    className: {
      description: "Allows additional styling to be applied to the button.",

      type: "string",
    },
   
  }}
/>







