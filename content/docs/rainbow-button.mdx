---
title: Rainbow Button
description: A vibrant button with an animated rainbow gradient border and subtle glow, adding a playful pop of color on hover and interaction.
---

import RainbowButtonPreview from "@/components/buttoncn-preview/rainbow-button-preview";
import { TypeTable } from "fumadocs-ui/components/type-table";

<Preview link="rainbow-button">
  <RainbowButtonPreview />
</Preview>

## Installation

```npm
npx shadcn@latest add http://localhost:3000/r/rainbow-button.json
```

## Usage

```tsx title="usage.tsx"
import RainbowButton from "../buttoncn/rainbow-button";

const RainbowButtonPreview = () => {
  return <RainbowButton>Rainbow Button</RainbowButton>;
};

export default RainbowButtonPreview;
```

### API Reference

### Rainbow Button

<TypeTable
  type={{
    sweepColor: {
description: 'Defines the color of the animated sweep wave and the button borders. The text color inherits this value by default, but can be customized using className.',
     
      type: 'hexcode',
      default: '#22d3ee',
    },
    backgroundColor: {
      description: 'Color of the button background',
     
      type: 'hexcode',
      default: '#083344',
    },
     className: {
      description: "Allows additional styling to be applied to the button.",

      type: "string",
    },
    children: {
      description: "The content of the button.",
      type: "React.ReactNode",
    },

}}
/>
